// src/commands/init.js

export default function initCommand(projectPath, options) {
  console.log("🔧 Init command triggered!");
  console.log("Project path:", projectPath);
  console.log("Options:", options);
}
