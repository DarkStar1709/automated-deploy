// src/commands/generateCICD.js

export default function generateCICDCommand(projectPath, options) {
  console.log("⚙️ Generate CI/CD command triggered!");
  console.log("Project path:", projectPath);
  console.log("Options:", options);
}
